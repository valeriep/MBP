function Massif(id, oid, nom, statut, LesPaysId, descr)
 {
	 var id ;
	 var oid ;
	 var nom ;
	 var statut ;
	 var LesPaysId ;
	 var descr ;

	 // Constructeur
	 this.id = id;
	 this.oid = oid;
	 this.nom = nom;
	 this.statut = statut;
	 this.LesPaysId = LesPaysId;
	 this.descr = descr;
 }