function Massif(id, oid, nom, statut, descr)
 {
	 var id ;
	 var oid ;
	 var nom ;
	 var statut ;
	 var descr ;

	 // Constructeur
	 this.id = id;
	 this.oid = oid;
	 this.nom = nom;
	 this.statut = statut;
	 this.descr = descr;
 }