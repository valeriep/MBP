"use strict";

/**
 * 
 * @constructor
 * @author ch4mp@c4-soft.com
 */
mbp.ResortListWidget = function(hookSelector) {
    mbp.Widget.call(this, '#dot-resort-list', hookSelector);// parent constructor

    Object.preventExtensions(this);
};